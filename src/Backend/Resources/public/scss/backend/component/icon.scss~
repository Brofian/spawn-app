.icon {

	display: inline-block;
	width: 1rem;
	height: 1rem;
	stroke: $white;
	fill: $black;


	svg {
		width: 100%;
		height: 100%;
	}

}

.icon {
	background-position: center;
	background-repeat: no-repeat;

	&-info {
		background-image: url("#{$icon-path}/config.svg");
		@extend .icon;
	}

	&-warning {
		background-image: url("#{$icon-path}/warning.svg");
		@extend .icon;
	}

	&-error {
		background-image: url("#{$icon-path}/warning.svg");
		@extend .icon;
	}

}